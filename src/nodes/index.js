const createTable = require('./createTable');
const insertNode = require('./insert');
const getNodes = require('./getNodes');
const getNodeData = require('./getNodeData');

module.exports = {
    createTable,
    insertNode,
    getNodes,
    getNodeData,
};
